{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"outputs": [".svelte-kit/**", "target/**"],
			"passThroughEnv": ["SENTRY_AUTH_TOKEN"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}